用时：断断续续的做了两天
问题：1.想循环让参数不断做除法，没办法的是不懂如何知道循环次数。 解决：使用while。
	2.得到的余数总是不正确，解决：仔细看了转化规则，发现得到余数应该用倒数排序，这用才能得到正确的值。
	3.slice得不到正确的字符串位数总少一位，解决：只输入开始位数，这样slice就能直接截取输入位数到结束。

总结：写了两天代码写的挺臃肿的，遇到问题基本都解决，完成了哈哈。